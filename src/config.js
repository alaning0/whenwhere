/**
 * WhenWhere Frontend Configuration
 */

// API server URL - can be overridden via environment variable during build
export const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:3002';
